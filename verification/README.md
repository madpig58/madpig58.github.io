The site is publicly available at https://madpig58.github.io.

I verified that the page loads correctly both in a browser and through a direct fetch request.

This check would have failed if the deployed site did not match the version I tested locally or if GitHub Pages was not serving the site correctly.

